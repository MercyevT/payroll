
<div id='app1' v-cloak>

    <v-app>
        <v-main>
             <v-btn
              @@click='add_workingtime'
             >
                 <v-icon>
                   mdi-plus
                 </v-icon>
                 
             </v-btn>
             <v-data-table
             :headers ='headers'
             :items   ='workingTimes'
              class='elevation-1'
             />
                  <template v-slot:item.actions='{item}'>
                        <v-btn 
                        class="white--text"
                        color="blue darken-1"
                        @@click="edit_workingtime(item)"
                        >
                            <v-icon>
                            mdi-pencil
                            </v-icon>
                            
                        </v-btn>
                </template>
             </v-data-table>
        </v-main>
    </v-app>
</div>
@section scripts{
    <script>
        var app;
            var component = {
                vuetify: new Vuetify()
                ,
                el:'#app1'
                ,
                data:{
              
                   workingTimes:[]
                   ,
                   headers:[

                      {text:'departmentId',value:'departmentId',align:'center',sortable:true},

                      {text:'start',value:'start',align:'center',sortable:true},
                      {text:'finish',value:'finish',align:'center',sortable:true},
                      {text:'actions',value:'actions',align:'center',sortable:true}

					]
                }//edata
                ,
                created(){
                  this.workingTimes = @Html.Raw(Json.Serialize(@ViewBag.workingTimes));
                }//ecreated
                ,
                methods:{
                    add_workingtime(){
                        window.location= '/workingtime/add';
                  
                        
                    }//ef
                    
                    ,
                    edit_workingtime(item){
                        window.location = '/workingtime/edit/'+item.workingTimeId;
                    }

                }//emethods
                ,
                computed:{

                }//ecomputed
            };
            app = new Vue(component);


    </script>

}